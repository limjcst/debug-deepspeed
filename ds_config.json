{
    "train_micro_batch_size_per_gpu": 64,
    "gradient_accumulation_steps": 1,
    "optimizer": {
        "type": "Adam",
        "params": {
            "lr": 0.0001,
            "weight_decay": 0.0001
        }
    },
    "fp16": {
        "enabled": false
    },
    "zero_optimization": {
        "stage": 0
    }
}
